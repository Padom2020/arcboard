# =============================================================================
# ARC Blockchain Platform - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env or .env.local files to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# For development with SQLite (no separate database server needed)
# DATABASE_URL="file:./dev.db"

# For development/production with PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/arc_platform?schema=public"

# Alternative PostgreSQL connection string format:
# DATABASE_URL="postgresql://username:password@host:port/database?schema=public"

# For production (e.g., Vercel Postgres, Supabase, etc.)
# DATABASE_URL="your-production-database-url"

# -----------------------------------------------------------------------------
# OpenAI API Configuration
# -----------------------------------------------------------------------------
# Required for AI Assistant and Debugging features
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-your-openai-api-key-here"

# Optional: Specify OpenAI model (defaults to gpt-3.5-turbo)
# OPENAI_MODEL="gpt-4"

# Optional: OpenAI organization ID (if using organization account)
# OPENAI_ORG_ID="org-your-organization-id"

# -----------------------------------------------------------------------------
# OpenZeppelin API Configuration
# -----------------------------------------------------------------------------
# Required for Smart Contract Generator feature
# Note: OpenZeppelin Contracts Wizard may not require an API key
# Check documentation: https://docs.openzeppelin.com/
OPENZEPPELIN_API_KEY="your-openzeppelin-api-key-here"

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Public URL of your application (used for API calls and redirects)
# Development:
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Production:
# NEXT_PUBLIC_APP_URL="https://your-domain.com"

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
# Options: development, production, test
NODE_ENV="development"

# -----------------------------------------------------------------------------
# Optional: Analytics and Monitoring
# -----------------------------------------------------------------------------
# Add your analytics/monitoring service keys here if needed
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"
# SENTRY_DSN="your-sentry-dsn"

# -----------------------------------------------------------------------------
# Optional: Email Service (for notifications)
# -----------------------------------------------------------------------------
# If you plan to add email notifications for DApp submissions
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@example.com"
# SMTP_PASSWORD="your-email-password"
# SMTP_FROM="noreply@your-domain.com"

# -----------------------------------------------------------------------------
# Optional: Rate Limiting
# -----------------------------------------------------------------------------
# Configure rate limiting for API endpoints
# RATE_LIMIT_WINDOW_MS="60000"  # 1 minute
# RATE_LIMIT_MAX_REQUESTS="10"

# -----------------------------------------------------------------------------
# Optional: Session and Security
# -----------------------------------------------------------------------------
# If you add authentication in the future
# NEXTAUTH_URL="http://localhost:3000"
# NEXTAUTH_SECRET="your-nextauth-secret-here"
# JWT_SECRET="your-jwt-secret-here"

# -----------------------------------------------------------------------------
# IMPORTANT SECURITY NOTES:
# -----------------------------------------------------------------------------
# 1. NEVER commit files containing actual API keys or secrets
# 2. Use different values for development and production
# 3. Rotate API keys regularly
# 4. Use environment-specific .env files (.env.local, .env.production.local)
# 5. Keep your .env files in .gitignore
# 6. Use a secrets manager for production (Vercel Secrets, AWS Secrets Manager, etc.)
# =============================================================================

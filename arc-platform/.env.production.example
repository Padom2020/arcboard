# =============================================================================
# ARC Blockchain Platform - Production Environment Variables
# =============================================================================
# This file shows the required environment variables for production deployment
# Copy these to your hosting platform's environment variable settings
# NEVER commit actual production values to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Production PostgreSQL connection string
# Examples:
# - Vercel Postgres: postgres://default:xxx@xxx-pooler.aws.neon.tech:5432/verceldb?sslmode=require
# - Supabase: postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# - Railway: postgresql://postgres:[password]@[host].railway.app:5432/railway
# - Neon: postgresql://[user]:[password]@[host].neon.tech/[db]?sslmode=require

DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# -----------------------------------------------------------------------------
# OpenAI API Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Production OpenAI API key - MUST be different from development key
# Get from: https://platform.openai.com/api-keys
# Recommended: Create a separate key for production with usage limits

OPENAI_API_KEY="sk-proj-your-production-openai-api-key-here"

# Optional: Specify model (gpt-4 for better quality, gpt-3.5-turbo for lower cost)
OPENAI_MODEL="gpt-3.5-turbo"

# Optional: Organization ID if using OpenAI organization account
# OPENAI_ORG_ID="org-your-organization-id"

# -----------------------------------------------------------------------------
# Application Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Your production domain - MUST match your actual deployment URL
# Examples:
# - Vercel: https://arc-platform.vercel.app
# - Custom domain: https://arc.yourdomain.com
# - Netlify: https://arc-platform.netlify.app

NEXT_PUBLIC_APP_URL="https://your-production-domain.com"

# -----------------------------------------------------------------------------
# OpenZeppelin API Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Required only if OpenZeppelin Contracts Wizard requires authentication
# Check: https://docs.openzeppelin.com/

OPENZEPPELIN_API_KEY="your-production-openzeppelin-api-key-here"

# -----------------------------------------------------------------------------
# Environment (AUTO-SET by most platforms)
# -----------------------------------------------------------------------------
NODE_ENV="production"

# -----------------------------------------------------------------------------
# Optional: Analytics and Monitoring
# -----------------------------------------------------------------------------
# Google Analytics
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# Sentry Error Tracking
# SENTRY_DSN="https://xxx@xxx.ingest.sentry.io/xxx"
# SENTRY_ORG="your-org"
# SENTRY_PROJECT="arc-platform"
# SENTRY_AUTH_TOKEN="your-auth-token"

# Vercel Analytics (automatically enabled on Vercel)
# No configuration needed

# -----------------------------------------------------------------------------
# Optional: Rate Limiting Configuration
# -----------------------------------------------------------------------------
# Configure API rate limits to prevent abuse
# RATE_LIMIT_WINDOW_MS="60000"  # 1 minute window
# RATE_LIMIT_MAX_REQUESTS="10"  # Max requests per window
# RATE_LIMIT_AI_MAX_REQUESTS="5"  # Stricter limit for AI endpoints

# -----------------------------------------------------------------------------
# Optional: Email Service (Future Feature)
# -----------------------------------------------------------------------------
# For DApp submission notifications
# SMTP_HOST="smtp.sendgrid.net"
# SMTP_PORT="587"
# SMTP_USER="apikey"
# SMTP_PASSWORD="your-sendgrid-api-key"
# SMTP_FROM="noreply@yourdomain.com"

# -----------------------------------------------------------------------------
# Optional: Authentication (Future Feature)
# -----------------------------------------------------------------------------
# If you add NextAuth.js or similar
# NEXTAUTH_URL="https://your-production-domain.com"
# NEXTAUTH_SECRET="your-production-nextauth-secret-minimum-32-characters"
# JWT_SECRET="your-production-jwt-secret-minimum-32-characters"

# -----------------------------------------------------------------------------
# Optional: Feature Flags
# -----------------------------------------------------------------------------
# Enable/disable features in production
# NEXT_PUBLIC_ENABLE_DEBUG_ASSISTANT="true"
# NEXT_PUBLIC_ENABLE_CONTRACT_GENERATOR="true"
# NEXT_PUBLIC_ENABLE_DAPP_SUBMISSION="true"

# -----------------------------------------------------------------------------
# PRODUCTION SECURITY CHECKLIST:
# -----------------------------------------------------------------------------
# ✅ Use different API keys than development
# ✅ Enable SSL/TLS for database connections (?sslmode=require)
# ✅ Set up API usage alerts in OpenAI dashboard
# ✅ Configure rate limiting for API endpoints
# ✅ Enable monitoring and error tracking
# ✅ Set up database backups
# ✅ Use environment variable encryption (Vercel does this automatically)
# ✅ Rotate API keys every 90 days
# ✅ Monitor API usage and costs regularly
# ✅ Set up billing alerts
# ✅ Review security logs periodically
# =============================================================================

# -----------------------------------------------------------------------------
# DEPLOYMENT PLATFORM SPECIFIC NOTES:
# -----------------------------------------------------------------------------

# VERCEL:
# - Add these variables in: Project Settings → Environment Variables
# - Select "Production" environment
# - Vercel automatically sets NODE_ENV=production
# - DATABASE_URL can be auto-populated if using Vercel Postgres

# NETLIFY:
# - Add in: Site Settings → 